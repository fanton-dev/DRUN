import 'package:flutter/material.dart';

class CodeInputPage extends StatelessWidget {
  final String error;

  const CodeInputPage({Key key, this.error}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Placeholder(),
    );
  }
}
